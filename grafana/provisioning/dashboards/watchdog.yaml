apiVersion: 1

providers:
  - name: smart-watchdog
    orgId: 1
    folder: Smart Watchdog
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
